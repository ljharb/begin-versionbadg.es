## https://versionbadg.es

Pretty basic SVG generation service.

For any github repo `$OWNER/$REPO` containing a `package.json`, `https://versionbadg.es/$OWNER/$REPO.svg` will produce an SVG badge of the current version.
